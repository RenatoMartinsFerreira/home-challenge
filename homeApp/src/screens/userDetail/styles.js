import {StyleSheet} from 'react-native';
import {normalize} from 'homeapp/src/constants';

export default StyleSheet.create({
  image: {
    width: normalize(100),
    aspectRatio: 1,
    borderRadius: normalize(100),
  },
});
