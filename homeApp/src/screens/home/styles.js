import {StyleSheet} from 'react-native';
import {colors} from 'homeapp/src/constants/colors';

export default StyleSheet.create({
  backgroundStyle: {
    flex: 1,
    backgroundColor: colors.white,
  },
});
